#include <iostream>
using namespace std;

int main() {

    int arr[3][3] = {
        {1, 2, 3},
        {4, 8, 2},
        {5, 1, 1}
    };

    int maxSum = 0;
    int maxRow = 0;

    for(int i = 0; i < 3; i++) {

        int sum = 0;

        for(int j = 0; j < 3; j++) {
            sum += arr[i][j];
        }

        if(sum > maxSum) {
            maxSum = sum;
            maxRow = i;
        }
    }

    cout << "Row index with maximum sum: " << maxRow << endl;

    return 0;
}
